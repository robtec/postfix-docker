version: '3'

services:
  mail:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: postfix-mail
    ports:
      - 25:25
    environment:
      MY_DOMAIN: localhost
    volumes:
      - ./mail:/var/spool/mail